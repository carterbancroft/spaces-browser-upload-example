'use strict'

const spaces = {
  spaceName: 'your space name',
  url: 'your space URL', // like sfo.digitaloceanspaces.com
  accessKeyId: 'your access key ID', // Don't commit your key
  secretAccessKey: 'your secret access key' // Don't commit your secret
}

module.exports = {
  spaces,
}
